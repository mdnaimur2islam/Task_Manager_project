import 'package:flutter/material.dart';

class center_circular_indicator extends StatelessWidget {
  const center_circular_indicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
